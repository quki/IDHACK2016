<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pure.gothic.hackathon.idhackandroid">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".volley.AppController"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/MyMaterialTheme">
        <activity android:name=".login.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ChatBubbleActivityForDoctor"
            android:label="@string/title_activity_chat_bubble" />
        <activity
            android:name=".ChatMessage"
            android:label="@string/title_activity_chat_message" />
        <activity
            android:name=".ChatBubbleActivityForPatient"
            android:label="@string/title_activity_chat_bubble_activity_for_patient"></activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".login.RegisterActivity" />
        <activity android:name=".MainActivityDoctor" />
        <activity android:name=".SubActivity" />
        <activity android:name=".SubActivityDoctor"></activity>
    </application>

</manifest>
